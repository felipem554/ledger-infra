name: Release to Production

# Deploys a tagged release of the ledger-service image (published to GHCR by the
# ledger-service repo's CI). This repo owns the chart and the kube credentials;
# the app repo owns build/publish. See ledger-service docs/ci-architecture.md.

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

env:
  IMAGE: ghcr.io/felipem554/ledger-service

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Set up Helm
        uses: azure/setup-helm@v4
      - name: Configure kubectl
        uses: azure/k8s-set-context@v4
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG_PROD }}
      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      - name: Deploy with Helm
        run: |
          helm upgrade --install ledger helm/ledger \
            --namespace ledger-prod \
            --create-namespace \
            -f helm/ledger/values.yaml \
            -f helm/ledger/values-prod.yaml \
            --set image.repository=${{ env.IMAGE }} \
            --set image.tag=${{ steps.version.outputs.VERSION }} \
            --set secretEnv.POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD_PROD }} \
            --atomic \
            --timeout 10m \
            --wait

  post-deploy-verify:
    name: Post-Deploy Verification
    runs-on: ubuntu-latest
    needs: deploy-production
    steps:
      - uses: actions/checkout@v4
      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D68
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update && sudo apt-get install k6
      - name: Run smoke test against production
        run: |
          BASE_URL=${{ secrets.PROD_URL }} k6 run k6/smoke.js
      - name: Verify health endpoint
        run: |
          curl -sf ${{ secrets.PROD_URL }}/healthz || exit 1
          curl -sf ${{ secrets.PROD_URL }}/readyz || exit 1
